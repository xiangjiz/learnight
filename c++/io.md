```c++
stdin;  //标准输入流，键盘输入；
stdout;  //标准输出流，屏幕打印；
stderr;  //标准错误流；

//字符IO函数：
int getchar();  //从键盘读取一个字符；
int fgetc(FILE fp);  //从文件fp中读取一个字符；
int put char(int ch);  //向屏幕打印字符ch；
int fputc(int c, FILE fp);  //向文件fp写入字符c；

//字符串IO函数：
char gets(char)




```

